# Required: Get these from https://www.strava.com/settings/api
CLIENT_ID=YOUR_CLIENT_ID                  # Your Strava API application's Client ID
CLIENT_SECRET=YOUR_CLIENT_SECRET          # Your Strava API application's Client Secret

# Optional: These will be automatically managed by the script
ACCESS_TOKEN=                             # Short-lived access token (auto-refreshed)
REFRESH_TOKEN=                            # Long-lived refresh token (auto-updated) 